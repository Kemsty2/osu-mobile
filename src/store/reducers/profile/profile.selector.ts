export const selectProfile = (state: any) => state.profile.profile;
export const selectProfileToken = (state: any) => state.profile.token;
export const selectProfileRefreshToken = (state: any) =>
  state.profile.refreshToken;
